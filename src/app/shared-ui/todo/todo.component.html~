<div class="todo-container">
  <h1>Todo Anwendung</h1>
  <div class="todo-input-container">
    <mat-form-field class="example-full-width">
      <mat-label>Neue Aufgabe eingeben</mat-label>
      <input class="todo-input" matInput placeholder="Aufgabe">
    </mat-form-field>
  </div>
  <div class="btn-buttons">
    <button class="btn-add">Add new Task</button>
    <button class="btn-add">All Tasks</button>
    <button class="btn-add">Completed Tasks</button>
    <button class="btn-add">Uncompleted Tasks</button>
  </div>
</div>
<div>
  <div class="hero-table-container">
    @for (todo of todos; track todo.id) {
      <app-todo-list [todo]="todo" (deleteTodo)="deleteTodo($event)" ></app-todo-list>
    }
  </div>
</div>


